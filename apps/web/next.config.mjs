/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      "raw.githubusercontent.com",
      "cdn.jsdelivr.net",
      "arweave.net",
      "cryptologos.cc",
    ],
  },
};

export default nextConfig;
